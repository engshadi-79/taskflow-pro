# TaskFlow Pro — نظام إدارة مهام الموظفين
## ملف الموجّه الرئيسي للتنفيذ (ضعه في جذر المشروع باسم PROJECT_PLAN.md أو CLAUDE.md)

---

## 1. نظرة عامة على المشروع

نظام ويب + تطبيق جوال لإدارة مهام الموظفين، بثلاثة مستويات صلاحيات (مدير عام، مدير قسم، موظف)، مع لوحة تحكم احترافية، إشعارات، تقارير قابلة للتصدير، وتحويل لاحق إلى تطبيق موبايل عبر Capacitor.

**الهدف من هذا الملف:** أن يُستخدم كمرجع تنفيذي كامل — أي مطوّر أو أداة AI تقرأه يجب أن تقدر تبني المشروع بدون الحاجة لسؤال المستخدم عن التفاصيل الأساسية، لأنها كلها موثقة هنا.

---

## 2. المكدس التقني (كله مجاني)

| الغرض | الأداة |
|---|---|
| الواجهة الأمامية | Next.js 14 (App Router) + TypeScript + Tailwind CSS |
| قاعدة البيانات + Auth | Supabase (PostgreSQL + Row Level Security) |
| تخزين الملفات | Supabase Storage |
| الإشعارات الفورية داخل التطبيق | Supabase Realtime |
| Push Notifications للموبايل | OneSignal (مجاني، أسهل تكامل من Firebase مع Capacitor) |
| الاستضافة | Vercel (نشر تلقائي من GitHub) |
| التحكم بالإصدارات | GitHub |
| تحويل لتطبيق موبايل | Capacitor (Android + iOS) من نفس كود الويب |
| تصدير التقارير | jsPDF أو exceljs (مكتبات مجانية) |
| الإعلانات (نسخة مجانية من التطبيق) | Google AdMob (مجاني، عبر Capacitor plugin) |

---

## 3. الأدوار والصلاحيات (RBAC)

### مدير عام (super_admin)
صلاحية كاملة: إنشاء/حذف موظفين ومدراء وأقسام، إنشاء وتعديل وحذف أي مهمة، اعتماد أو رفض أي مهمة منجزة، مشاهدة كل التقارير وسجل النشاط، تصدير البيانات، إرسال تنبيهات جماعية.

### مدير قسم (department_manager)
يرى فقط موظفي قسمه. يمكنه إنشاء مهام لهم، تعديلها، اعتماد أو رفض الإنجاز، مشاهدة تقارير قسمه فقط.

### موظف (employee)
يرى فقط مهامه الشخصية، إشعاراته، تقويمه. لا يستطيع رؤية أي موظف آخر أو مهامه.

**قاعدة أساسية:** الصلاحيات تُطبَّق على مستوى قاعدة البيانات عبر Supabase RLS، وليس فقط في واجهة المستخدم — أي طلب API يجب أن يُرفض تلقائيًا من القاعدة نفسها إذا لم يملك الصلاحية، بغض النظر عن الواجهة.

---

## 4. مخطط قاعدة البيانات (الجداول الأساسية)

```
organizations
  id, name, created_at
  -- عمود موجود من البداية لدعم SaaS مستقبلًا حتى لو حاليًا منظمة واحدة فقط

users (تمتد من Supabase auth.users)
  id, organization_id, full_name, email, phone,
  role (super_admin | department_manager | employee),
  department_id, job_title, is_active, created_at

departments
  id, organization_id, name, manager_id

tasks
  id, organization_id, title, description,
  assigned_to (user_id), created_by (user_id),
  priority (low | medium | high | urgent),
  status (new | in_progress | pending_review | completed | overdue | cancelled),
  start_date, start_time, due_date, due_time,
  is_recurring, recurrence_pattern (daily | weekly | monthly | yearly | null),
  created_at, updated_at

task_attachments
  id, task_id, uploaded_by, file_url, file_name, file_type, uploaded_at

task_comments
  id, task_id, user_id, content, created_at

notifications
  id, user_id, task_id, type, message, is_read, created_at

activity_log
  id, organization_id, user_id, action_type, entity_type, entity_id,
  description, created_at

task_templates
  id, organization_id, title, description, default_priority, created_by
```

### سياسات RLS (المبدأ العام لكل جدول حساس)
- `super_admin`: وصول كامل لكل صفوف `organization_id` الخاص به.
- `department_manager`: وصول فقط للصفوف حيث `department_id` يطابق قسمه.
- `employee`: وصول فقط للصفوف حيث `assigned_to = auth.uid()` أو `user_id = auth.uid()`.

---

## 5. حالات المهمة (Task States)

```
جديدة → قيد التنفيذ → بانتظار المراجعة → مكتملة
                                      ↘ (رفض) → قيد التنفيذ (مع ملاحظات)
متأخرة: تُحسب تلقائيًا إذا تجاوز due_date/due_time دون اكتمال
ملغاة: يمكن للمدير إلغاؤها في أي وقت
```

---

## 6. مراحل التنفيذ (كل مرحلة تُبنى وتُختبر كاملة قبل الانتقال للتالية)

### المرحلة 1 — الأساس
- هيكلة مشروع Next.js + TypeScript + Tailwind
- نظام الألوان والخطوط (هوية بصرية مخصصة — راجع قسم 7)
- صفحات تسجيل الدخول/الخروج مرتبطة بـ Supabase Auth
- Layout عام يتغير حسب الدور (super_admin / department_manager / employee)

### المرحلة 2 — قاعدة البيانات
- ملف SQL كامل لإنشاء الجداول (قسم 4)
- كل سياسات RLS جاهزة للصق المباشر في Supabase SQL Editor
- بيانات تجريبية (seed data) لتجربة الأدوار الثلاثة

### المرحلة 3 — لوحة تحكم المدير العام
- Dashboard: عدد الموظفين، المهام النشطة/المتأخرة/المنجزة، نسبة الإنجاز
- إدارة المستخدمين (إضافة/تعديل/تعطيل)
- إدارة الأقسام
- إنشاء/تعديل/حذف المهام مع كل الحقول (أولوية، تواريخ، مرفقات، ملاحظات)

### المرحلة 4 — واجهة الموظف ومدير القسم
- عرض المهام حسب الدور
- رفع الملفات، إضافة تعليقات
- زر "تم الإنجاز" → حالة "بانتظار اعتماد المدير"
- اعتماد/رفض المهمة من المدير مع ملاحظات

### المرحلة 5 — الإشعارات والتقارير
- إشعارات داخل التطبيق عبر Supabase Realtime
- تنبيهات مجدولة قبل الموعد (أسبوع/3 أيام/يوم/ساعتين/نصف ساعة)
- تقارير: أكثر موظف إنجازًا، أكثر قسم إنتاجية، متوسط وقت الإنجاز، نسبة التأخير
- تصدير PDF / Excel

### المرحلة 6 — تحويل لتطبيق موبايل
- إعداد Capacitor (Android + iOS) من نفس كود الويب
- دمج OneSignal لـ Push Notifications
- دمج AdMob (بانر + إعلان بيني محدود) — يظهر فقط لمنظمات الخطة المجانية، بأوضاع اختبار حتى تربط حسابك الحقيقي
- اختبار الوصول للكاميرا ورفع الملفات من الجوال

---

## 7. الهوية البصرية (Design Direction)

**محدّثة — تم استبدال الهوية الأصلية (محايدة + لون واحد) بتصميم "منجز" الملوّن بناءً على تصاميم HTML قدّمها المستخدم لاحقًا في المشروع.**

- **الطابع العام:** احترافي وحيوي، بطاقات ملوّنة بتدرجات (gradients) لكل حالة/قسم
- **الألوان:** خلفية محايدة باردة (`#F3F5FB`) + خمس عائلات ألوان بتدرجات: أزرق (اللون الأساسي/العلامة)، برتقالي، وردي، أخضر، بنفسجي — تُستخدم للبطاقات الإحصائية والوسوم والحالات
- **الخطوط:** Cairo للعناوين (600-900) + Tajawal للنصوص (300-800)
- **دعم كامل للعربية (RTL)** — الواجهة عربية بالكامل حاليًا
- **الوضع الليلي (Dark Mode)** كخيار وليس افتراضي (تدرجات الألوان الملوّنة تبقى كما هي، الخلفيات المحايدة فقط تتغيّر)
- **الاستجابة الكاملة (Responsive)** لأن اللوحة نفسها ستتحول لتطبيق موبايل لاحقًا
- **ميزتان جديدتان أُضيفتا مع التصميم الجديد:** لوحة كانبان بسحب وإفلات حقيقي (`/dashboard/kanban`)، وصفحة ملف شخصي لكل عضو (`/dashboard/profile/[id]`) تجمع إحصائياته الحقيقية ومهامه الحالية

---

## 7.5 نموذج الإعلانات (AdMob) — مرتبط بخطة SaaS

**المبدأ:** الإعلانات تظهر فقط للمنظمات على الخطة المجانية (Free tier)، وتختفي تلقائيًا لأي منظمة مشتركة (Paid tier). هذا يتطلب:

- عمود `plan_type` (`free` | `paid`) في جدول `organizations`
- شرط في الواجهة: تحميل مكوّن الإعلان فقط إذا `organization.plan_type === 'free'`
- الإعلانات تظهر **فقط داخل تطبيق الموبايل (Capacitor)** عبر AdMob، وليس داخل لوحة تحكم الويب — لأن AdMob مخصص لتطبيقات الجوال، والمستخدمين يديرون بيانات عمل حساسة على الويب حيث الإعلانات تُشتت وتقلل الاحترافية.
- أماكن اقتراحية للإعلانات: بانر ثابت أسفل شاشة "مهامي" (غير متداخل مع الأزرار الأساسية)، وإعلان بيني (Interstitial) بحد أقصى مرة كل عدة جلسات — لا يظهر أبدًا أثناء تعبئة نموذج أو رفع ملف حتى لا يقطع تدفق العمل.

**مكتبة التنفيذ:** `@capacitor-community/admob` (مجانية مفتوحة المصدر، متوافقة مع Capacitor).

**يحتاج تدخلك لاحقًا:** إنشاء حساب AdMob وربط تطبيقك به للحصول على App ID و Ad Unit IDs — هذه خطوة تسجيل حساب لا أقدر أنفذها بدلاً عنك، لكن سأجهز كل الكود بحيث تضع هذه المعرّفات في ملف الإعدادات (`.env`) فقط دون أي تعديل إضافي.

---

## 8. ملاحظات تنفيذية مهمة (لا تتجاهلها)

1. لا تبني منطق الصلاحيات في الواجهة فقط — RLS في القاعدة هو خط الدفاع الحقيقي.
2. عمود `organization_id` موجود من أول جدول حتى لو المشروع لعميل واحد الآن — يسهّل التحول لـ SaaS لاحقًا.
3. Supabase المجاني يوقف المشروع تلقائيًا بعد 7 أيام خمول — يُذكر المستخدم عند الانتقال من التطوير للاستخدام الفعلي.
4. Supabase وحده لا يرسل Push Notifications للهاتف عند إغلاق التطبيق — إلزامي دمج OneSignal لهذا الغرض.
5. حدود Vercel المجانية (وقت تنفيذ الدوال، عرض النطاق) كافية للتطوير والاستخدام الأولي، لكن يجب مراقبتها إذا زاد عدد المستخدمين أو أصبح تصدير التقارير ثقيلًا.
6. الإعلانات (AdMob) تُبنى في الكود من المرحلة 6 فقط (تطبيق الموبايل)، لكنها لن تُفعَّل فعليًا إلا بعد ربط حساب AdMob الحقيقي من طرفك — قبل ذلك تعمل في "وضع تجريبي" (Test Ad Units) بدون أي أرباح حقيقية.

---

## 9. حالة المشروع الحالية

- [x] المرحلة 1: الأساس
- [x] المرحلة 2: قاعدة البيانات
- [x] المرحلة 3: لوحة تحكم المدير العام
- [x] المرحلة 4: واجهة الموظف ومدير القسم
- [x] المرحلة 5: الإشعارات والتقارير
- [ ] المرحلة 6: تحويل لتطبيق موبايل

*(حدّث هذا القسم كلما اكتملت مرحلة، حتى تبقى نقطة مرجعية لأي جلسة عمل جديدة)*
