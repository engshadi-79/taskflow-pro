# منجز (MONJEZ) — سجل المشروع

> هذا الملف سجل حيّ لكل ما يحدث في المشروع: القرارات، المراحل المكتملة، الأنماط المعتمدة، والحالة الراهنة. يُحدَّث مع كل تغيير جديد.

**آخر تحديث:** 2026-08-12

---

## 1. نظرة عامة

نظام إدارة مهام وموظفين عربي (RTL) بثلاثة مستويات صلاحيات (`super_admin`، `department_manager`، `employee`)، مبني على:

| الطبقة | التقنية |
|---|---|
| الواجهة | Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind CSS v4 |
| قاعدة البيانات + Auth + Storage + Realtime | Supabase (PostgreSQL + Row Level Security) |
| الذكاء الاصطناعي | Google Gemini (`gemini-3.5-flash-lite`) عبر `@google/genai` |
| الاستضافة | Vercel |
| المستودع | `github.com/engshadi-79/taskflow-pro`، الفرع `main` |

**النطاق الحالي:** جاري تنفيذ خطتين متتاليتين من المهام:
1. **MONJEZ 2.0** (18 مرحلة) — **مكتملة بالكامل**.
2. **MONJEZ V2** (25 مرحلة، مرقّمة P01–P25) — **قيد التنفيذ، آخر مرحلة مكتملة: P10**.

بالإضافة إلى طلبات تعديل مباشرة (تصميم، إصلاحات) بين المراحل.

---

## 2. حالة المراحل

### MONJEZ 2.0 — مكتملة (18/18)

| # | المرحلة | الملخص |
|---|---|---|
| 08 | SLA Engine | سياسات SLA، حساب المخالفات، تقارير الالتزام |
| 09 | Workflow Engine | محرك موافقات عام (تصعيد، إعادة تكليف، سجل قرارات) + إشعارات |
| 10 | Automation Engine | قواعد Trigger→Condition→Action، منع التكرار |
| 11 | Calendar | عرض يوم/أسبوع/شهر، سحب وإفلات لتغيير الموعد (Server Action حقيقي) |
| 12 | Meetings | اجتماعات، محاضر، تحويل بند محضر إلى مهمة |
| 13 | Knowledge Base | مقالات، تصنيفات، بحث، إشعار عند النشر فقط |
| 14 | Executive Dashboard 2.0 | KPIs حقيقية حسب الدور، Drill-down |
| 15 | Advanced Reporting | أداء الأقسام/الموظفين/المشاريع/الأولوية |
| 16 | AI Assistant | مساعد Gemini بصلاحيات مقيّدة، اقتراحات تحتاج تأكيدًا، سجل تدقيق |
| 17 | PWA / Mobile Readiness | Manifest، أيقونات، Service Worker لكاش الأصول الثابتة فقط |
| 18 | Security & Final QA | تدقيق شامل — وجد وأصلح ثغرة صلاحيات Storage لمرفقات المعرفة/الاجتماعات |

*(المراحل 1–7 نُفّذت في جلسات سابقة قبل بداية هذا السجل التفصيلي.)*

### MONJEZ V2 — قيد التنفيذ (10/25)

| # | المرحلة | الحالة | الملاحظات |
|---|---|---|---|
| P01 | Audit & Foundation | ✅ | `docs/MONJEZ_V2_AUDIT.md` — وجد ثغرة تصعيد صلاحيات حرجة |
| P02 | Security & Foundation Fixes | ✅ | أصلح `users_self_update` (كانت تسمح بتعديل `role` عبر REST مباشر) + تباين ألوان `text-faint` |
| P03 | Advanced Task Workflow | ✅ | (مكتملة، السياق الكامل في المحادثة الأصلية) |
| P04 | Recurring Tasks | ✅ | توليد فعلي للمهام المتكررة عبر pg_cron (كانت بيانات وصفية فقط بلا تنفيذ) |
| P05 | Calendar | ✅ | تقييد السحب بالصلاحية + ألوان للمهام المتأخرة/المتكررة |
| P06 | Projects | ✅ (مفحوصة، موجودة أصلًا) | — |
| P07 | Collaboration | ✅ | ردود، تعديل، إشارات @mention، مرفقات على التعليقات، Realtime |
| P08 | Attachments & Documents | ✅ | إصدارات (Versioning)، معاينة صور/PDF، تحقق حجم/نوع على الخادم |
| P09 | Dashboard V2 | ✅ | فلاتر فترة/قسم/مشروع/موظف على لوحة التحكم |
| P10 | Analytics V2 | ✅ | نسبة التأخير، متوسط وقت الإنجاز، معدل رفض المراجعة (جدول جديد)، اتجاه أسبوعي |
| P11 | Organization Settings | ⏳ لم تبدأ | |
| P12–P25 | — | ⏳ لم تبدأ | |

---

## 3. تعديلات مباشرة (خارج ترقيم المراحل)

طلبات تصميم/إصلاح نُفّذت مباشرة بين المراحل بأمر المستخدم:

- **إعادة تصميم صف الأدوات السريعة**: تخصيص كامل (إضافة/حذف/إعادة ترتيب/روابط خارجية)، محفوظ لكل مستخدم (`users.dashboard_shortcuts`).
- **بطاقة "متصلون الآن"**: حضور لحظي حقيقي عبر Supabase Realtime Presence (لا رقم مخزّن)، مع نافذة تعرض القائمة الفعلية.
- **زر "آخر أنشطتي"**: يعرض نشاط المستخدم من `activity_log` (كانت هذه الجداول غير مرئية للموظف العادي — أُضيفت سياسة RLS).
- **إعادة تصميم لافتة الترحيب**: الصورة الحقيقية (ليس الحرف الأول فقط)، ترتيب الصورة/الاسم/الدور/التاريخ حسب مخطط محدد، تحية وتاريخ يعتمدان على توقيت المتصفح الفعلي (لا توقيت الخادم UTC).
- **حذف صف الإحصاءات الست** ووضع صف Hero/KPI مكانه فوق الأدوات السريعة.

---

## 4. أخطاء حقيقية وُجدت وأُصلحت

سجل صريح لأخطاء تجاوزت الفحوصات المحلية (`tsc`/`lint`/`build`) وظهرت فقط عند التنفيذ الحقيقي على Vercel — لتجنّب تكرارها:

1. **نسيان `git add` لملف معدّل** (كوميت `ee76297`) — أدى لفشل بناء صامت على Vercel لعدة كوميتات متتالية بينما كانت Vercel تعيد نشر آخر نسخة ناجحة قديمة دون أي إشعار واضح. **الدرس:** دائمًا `git status` قبل الدفع، لا الاعتماد على "نجاح البناء المحلي" فقط.
2. **قناتان بنفس اسم Topic في Supabase Realtime Presence** — الأولى فقط تنجح، الثانية لا تُستدعى إطلاقًا بلا أي خطأ ظاهر. **الحل الدائم:** مكوّن واحد فقط يملك القناة (`PresenceTracker`)، والباقي يستمع فقط.
3. **استدعاء دالة عادية من ملف `"use client"` مباشرة من Server Component** — يمرّ البناء بنجاح لكنه ينهار عند الطلب الحقيقي فقط (`Attempted to call X() from the server but X is on the client`). **الحل الدائم:** أي دالة تُستخدم من الخادم والعميل معًا يجب أن تكون في ملف بلا أي Directive.
4. **تعارض فئتي Tailwind لنفس الخاصية في نفس الـclassName** (مثل `text-[12px]` و`text-[14px]` معًا) — يُحل بترتيب غير متوقع، فيبدو أن التعديل "لم يُطبَّق". **الحل:** لا تُبقِ القيمة الافتراضية والتخصيص في نفس السلسلة؛ اجعل التخصيص يستبدل الافتراضي بالكامل.
5. **معالج حدث (`onChange`) على Server Component بلا `"use client"`** — نفس فئة الخطأ رقم 3 (يمرّ البناء، ينهار عند التنفيذ الفعلي).
6. **RLS تُقيّد الصف لكن لا العمود** — سياسة `users_self_update` سمحت بتحديث أي عمود في الصف الذاتي، مما يسمح بتصعيد الصلاحيات عبر REST مباشر. **الحل الدائم:** أي تحديث ذاتي يمرّ عبر دالة `SECURITY DEFINER` تحدد الأعمدة صريحًا، لا سياسة `for update` عامة.

---

## 5. أنماط معتمدة (يجب اتباعها في أي إضافة جديدة)

- **لا صلاحيات جديدة**: إعادة استخدام `super_admin` / `department_manager` / `employee` دائمًا.
- **RLS هي التنفيذ الحقيقي**، لا الشرط داخل Server Action — الأخير للرسائل الواضحة فقط.
- **دوران RLS العكسي** (جدول A يستعلم عن B وB يستعلم عن A) يُحل بدالة `SECURITY DEFINER` تتحقق يدويًا من الرؤية.
- **لا سياسة INSERT مباشرة على `notifications`** — كل إشعار يُنشأ من دالة `SECURITY DEFINER` داخل Trigger.
- **تعديل الصف الذاتي في `users`** يمرّ فقط عبر دوال `update_own_profile` / `update_own_avatar` / `update_own_dashboard_shortcuts` (لا سياسة UPDATE عامة).
- **التواريخ**: حساب UTC صريح دائمًا في JS (`src/lib/calendar-dates.ts`) لتجنّب انزلاق اليوم.
- **الأيقونات**: SVG من `src/components/shared/icons.tsx` فقط — ممنوع الإيموجي كأيقونة.
- **الملفات المشتركة بين خادم وعميل** (دوال عادية بلا حالة) تعيش في ملف بلا أي Directive، مستوردة من الطرفين.
- **بعد كل تغيير**: `npx tsc --noEmit` ثم `npm run lint` ثم `npm run build`، وفحص `git status` قبل أي `git add`/commit للتأكد من عدم نسيان ملف.

---

## 6. فهرس ملفات SQL (بالترتيب التاريخي التقريبي)

الملفات الأساسية: `schema.sql`، `rls.sql`، `seed.sql`. كل ملف بعدها هو Migration يُشغَّل مرة واحدة، بالترتيب الظاهر أعلاه في مجلد `supabase/`. الأحدث زمنيًا (ضمن هذا السجل): `activity_log_own.sql`، `dashboard_shortcuts.sql`، `review_rejection_rate.sql`، `task_attachments_v2.sql`، `task_comments_v2.sql` + `task_comments_v2_fix.sql`، `recurring_tasks.sql`، `fix_self_profile_update.sql`.

---

## 7. التالي

المتابعة إلى **P11 — Organization Settings** عند طلب المستخدم "اكمل"، وفق بروتوكول الخطة (مرحلة واحدة فقط في كل مرة، انتظار الأمر قبل الانتقال للتالية).
