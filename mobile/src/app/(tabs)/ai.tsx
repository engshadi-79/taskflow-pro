import { ComingSoonScreen } from "@/components/coming-soon";

// Placeholder for the mockup's "MONJEZ AI" assistant screen - the home
// screen's gradient CTA links here. Full build (chat UI wired to the same
// backend as the web app's ai-assistant-panel.tsx) is its own later phase,
// since it depends on confirming the mobile app can reach that API.
export default function AiAssistantScreen() {
  return <ComingSoonScreen title="مساعد MONJEZ" />;
}
