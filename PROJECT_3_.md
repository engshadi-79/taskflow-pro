# MONJEZ 3.0 — حالة المراحل (موثّقة بالفحص المباشر للكود)

هذا الملف لم يكن موجودًا قبل هذا التاريخ. الحالة الموثّقة هنا جاءت من فحص مباشر
للكود الفعلي في `D:\ai\Mahamm` (لا من الذاكرة ولا من افتراض)، بعد أن تبيّن أن
مستند برومبتات سابق ("MONJEZ 3.0 — برومبتات المراحل المتبقية") كان قديمًا
ويصف عدة مراحل كفجوات رغم أنها منجزة فعليًا.

**قاعدة التحديث**: بعد كل مرحلة تُنفَّذ، يُحدَّث قسمها هنا (الحالة + أي فجوة
متبقية) بنفس الأسلوب، ثم توقف كامل بانتظار أمر "اكمل" صريح للمرحلة التالية.

---

## ✅ منجزة فعليًا (كانت موصوفة كفجوات في المستند القديم — تم التحقق المباشر)

### P12 — تفضيلات الإشعارات الشخصية
**منجزة بالكامل.** `supabase/notification_center_v3.sql` يضيف
`user_notification_preferences` بأعمدة `dnd_enabled`, `dnd_start_time`,
`dnd_end_time`, `dnd_timezone`، بالإضافة لدالة تفحص هذه القيم قبل إدراج أي
إشعار جديد — يغطي بالضبط ما طلبه P12 (Quiet Hours + DND). قناة واتساب
مضافة أيضًا عبر `whatsapp_notification_pref.sql`
(`whatsapp_notifications_enabled`). واجهة الإعدادات موجودة في
`src/lib/actions/notification-preferences.ts`.

### P14 — توسيع نطاق البحث الموحّد
**منجزة بالكامل.** `src/components/dashboard/command-palette.tsx` — نوع
`Kind` يشمل فعليًا `project`, `meeting`, `article` بالإضافة إلى
`task/employee/department`، مع بادئات نطاق (`PREFIXES`) لكل منها.

### P15 — البريد والتقارير المجدولة
**منجزة.** Resend مربوط فعليًا (`RESEND_API_KEY`)، ومهمتا cron حقيقيتان:
`src/app/api/cron/daily-digest-emails/route.ts` و
`src/app/api/cron/weekly-report-emails/route.ts`.

### P17 — الخطط والفوترة
**منجزة (بُنيت أغلبها في هذه الجلسة نفسها).** `plan_type` يُستخدم فعليًا في:
حد مقاعد الخطة المجانية (`src/lib/plans.ts`)، انتهاء فترة تجريبية 14 يوم
(`isTrialExpired`)، وإثبات دفع (تحويل بنكي/Binance) مع تفعيل يدوي من
مالك المنصة (`src/lib/actions/plans.ts`, `src/lib/actions/platform.ts`).
لا معالج دفع فعلي (Stripe) — قرار مالي مؤجَّل عمدًا، يطابق قيد المستند
الأصلي بعدم إدخال معالج دفع في هذه المرحلة.

---

## ⏳ فجوات حقيقية (تم التحقق المباشر أنها لا تزال ناقصة)

الترتيب أدناه هو ترتيب التنفيذ الفعلي المتبقي.

### P20 — Mobile: Offline Support for Tasks
**منجزة** (نطاق محدود بوعي: "التطبيق مفتوح أصلاً وينقطع الاتصال أثناء
الاستخدام" - وليس فتح التطبيق بلا اتصال من الأساس، بقرار صريح من المستخدم
لتفادي بنية تخزين مسبق معقّدة في Service Worker غير مبرَّرة لحجم هذا الجزء).

**ما بُني**: طابور تعديلات محلي عبر IndexedDB (`src/lib/offline/db.ts`,
`queue.ts`, `sync.ts`, `use-online-status.ts`) لثلاث عمليات على المهام من
الجوال فقط: إرسال للمراجعة، قرار المراجعة (قبول/رفض)، إضافة تعليق. كل
عملية تمر بنفس Server Action المستخدم أونلاين تمامًا (`submitForReview`,
`reviewTask`, `addComment` في `src/lib/actions/tasks.ts`) - لا مسار موازٍ،
لا تجاوز لأي فحص صلاحية/RLS موجود. عند الاتصال، تُزامَن العمليات المعلّقة
تلقائيًا (`useOnlineStatus` مربوط بحدثي `online`/`offline` عبر
`useSyncExternalStore`، مثل `theme-toggle.tsx`، ومُركَّب مرة واحدة في
`src/app/m/layout.tsx` عبر `MobileOfflineSync`، بنفس نمط `PresenceTracker`
في لوحة التحكم). كشف تعارض حقيقي: تعديلات الحالة تُقارَن بـ`tasks.updated_at`
وقت التزامن؛ إن تغيّرت المهمة على الخادم أثناء الانقطاع، لا تُطبَّق العملية
تلقائيًا - تُعرَض في `<TaskConflictAlert>` (`src/components/mobile/
offline-status.tsx`) مع خياري "تطبيق تغييري رغم ذلك" أو "تجاهل تغييري".
مؤشر الواجهة "غير متصل - سيُزامَن التغيير عند عودة الاتصال" يظهر عند أي
محاولة تعديل وقت الانقطاع.

**نطاق مُسقَط عمدًا**: عرض قائمة المهام من كاش محلي عند فتح التطبيق بلا
اتصال من الأساس (cold boot) - غير مبني، ويحتاج بنية تخزين مسبق لصفحات
كاملة في `public/sw.js` (تعقيد وخطر أعطال أعلى بكثير من الفائدة لهذا
النطاق الضيق، بقرار المستخدم).

**تحقّق التنفيذ**: `npx tsc --noEmit` و`npx eslint src/` نظيفان بالكامل.
آليات IndexedDB (فتح قاعدة، كتابة، قراءة) اختُبِرت مباشرة في متصفح حقيقي
وأعطت نتائج صحيحة. اختبار تفاعلي كامل (ضغط أزرار فعلية داخل تطبيق الجوال)
لم يكتمل بسبب نفس قيد أداة المعاينة الموثّق في الذاكرة
(`browser-pane-not-compositing`) - يحتاج تجربة فعلية من جهاز المستخدم.

### P16 — دعوة زميل إلى مؤسسة قائمة
**منجزة.** جدول `organization_invites` (كود عشوائي، دور، قسم اختياري، حد
استخدامات، تاريخ انتهاء) + دالة `redeem_organization_invite()` تُستدعى
بنفس آلية `claim_new_organization()` تمامًا (نداء منفصل بعد تسجيل الدخول،
من `/auth/callback` أو مباشرة داخل `signUpWithInvite`) — منطق
"صفر مؤسسات → إنشاء مؤسسة جديدة" و`handle_new_user()` لم يُلمَسا إطلاقًا.
صفحة عامة `/invite/[code]` تعرض اسم المؤسسة والدور المعروض وتقبل عبر
Google أو بريد/كلمة مرور. إدارة الدعوات (إنشاء/عرض/إلغاء) في
`/dashboard/employees` خلف `can.manageOrganization` (صلاحية `organization.
manage` الموجودة أصلاً، بدون صلاحية جديدة). اختُبِر التدفق كاملاً مباشرة
على قاعدة البيانات الحقيقية: إنشاء دعوة → تسجيل مستخدم جديد (يهبط بالمؤسسة
الأقدم تلقائيًا كالمعتاد) → استخدام الدعوة (ينتقل للمؤسسة الصحيحة بالدور
الصحيح ويُفعَّل فورًا) → محاولة استخدام ثانية لدعوة أحادية الاستخدام تفشل
كما هو متوقع بدون زيادة العدّاد.

**أثر جانبي معروف (موروث من `claim_new_organization` الموجودة أصلاً، لم
يُصلَح هنا لتفادي توسيع نطاق هذه المرحلة)**: لأن `handle_new_user()` تهبط
بأي تسجيل ذاتي بالمؤسسة الأقدم أولاً، مدراء تلك المؤسسة يستلمون إشعار
"بانتظار الموافقة" حتى لو كان المستخدم سينتقل فورًا لمؤسسة أخرى عبر
الدعوة. موجود مسبقًا بنفس الشكل في تدفّق "إنشاء مؤسسة جديدة" الحالي.

### P13 — مركز القرار التنفيذي (ربط بالمساعد الذكي)
**منجزة.** زر "اسأل المساعد عن هذا" على كل بطاقة في
`src/components/dashboard/insight-card.tsx` يرسل حدث `CustomEvent`
(`ASK_ASSISTANT_EVENT`، نفس نمط `presence-tracker.tsx` → `online-now-
widget.tsx` الموجود أصلاً) بنص جاهز مبني من `title`/`explanation` الحقيقيين
للبطاقة. `AiAssistantPanel` (مثبَّت مرة واحدة في تخطيط لوحة التحكم) يستمع
لهذا الحدث، يفتح نفسه، ويرسل السؤال تلقائيًا عبر نفس مسار `/api/ai/chat`
الحالي — بدون أي تعديل على أدوات المساعد الذكي (P09) نفسها. اختُبِر
مباشرة في المتصفح: الحدث فتح اللوحة، أرسل السؤال، ورجعت إجابة حقيقية
مبنية على بيانات المؤسسة الفعلية (أسماء أقسام وموظفين حقيقيين).

### P11 — إعادة محاولة تلقائية (Retry) عند فشل الأتمتة
**منجزة فعلاً — تصحيح لخطأ فحص سابق مني.** الفحص الأول (أعلاه) كان ناقصًا:
بحثت عن retry/attempt/backoff في `automation_engine.sql` فقط، وفاتني
`supabase/automation_center_v3.sql` (عنوانه الصريح داخل الملف: "MONJEZ 3.0
P11 — Automation Center") الذي يبني P11 كاملاً: أعمدة `attempt_count`/
`next_retry_at` على `automation_executions`، حالة `retry_pending`/`failed`،
دالة `retry_failed_automations()` (حتى 3 محاولات، تراجع 5 دقائق ثابت)
بمهمة cron خاصة بها (`retry-failed-automations`، كل 15 دقيقة) — بالإضافة
لمعرض 7 قوالب جاهزة (`automation_rule_templates` + `seed_default_
automation_templates()` + Trigger لتلقيم أي مؤسسة جديدة تلقائيًا). تحقّقت
مباشرة من قاعدة البيانات الحقيقية: الأعمدة موجودة، السبع قوالب فعلاً
مزروعة للمؤسسة الحقيقية بأسمائها بالضبط، والدالة تعمل بدون خطأ. **لا حاجة
لأي كود جديد** — هذا يغلق آخر فجوة حقيقية في خارطة الطريق المتبقية (P19
وFeature Flags مؤجّلتان عمدًا وما بقي غيرهما).

### P19 — التكاملات المؤسسية (Slack/Teams) — مؤجّلة بوعي
**حالة**: لم تبدأ، ومؤجّلة حسب توصية المستند الأصلي حتى استقرار P18
(الذي هو فعليًا منجز — `src/app/api/webhooks/notification-created`،
`src/app/dashboard/settings/developer`). لا تُبنى إلا بطلب صريح منفصل.

### Feature Flags — مؤجّلة بوعي
**حالة**: لم تبدأ، ولا `feature_flags` أو ما يشبهها في `supabase/*.sql`.
لا تُبنى إلا عند حاجة فعلية موثّقة أو طلب صريح، حسب فلسفة المشروع المعلنة.
